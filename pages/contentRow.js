import ContentCard from "./contentCard";

export default function ContentRow() {
  return (
      <div id={'box'} className={'mx-auto w-min py-10 h-[500px] overflow-hidden mt-[30px] '}>
        <ContentCard colour={'bg-sky-500'}/>
      </div>
  )
}